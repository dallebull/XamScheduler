<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:behaviors="clr-namespace:FriskaClient.Behaviors;assembly=FriskaClient"
             x:Class="FriskaClient.EditFacitPage">
    <ContentPage.Content>
        <Grid>
            <Image Aspect="AspectFill" Source="wallpaper.png"/>
            <StackLayout Margin="10" HorizontalOptions="CenterAndExpand" VerticalOptions="FillAndExpand">
                <Label Text="Redigera Kontroll"
               Style="{DynamicResource TitleStyle}"
               Margin="0, 20"
               HorizontalTextAlignment="Center" 
               TextColor="White" 
                FontAttributes="Bold"/>

                <!--<Label Text="Kontroll" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" FontAttributes="Bold"/>-->
                <Entry x:Name="kontrollEntry" Text="{Binding Kontroll}"  BackgroundColor="#50FFFFFF" Keyboard="Numeric" VerticalTextAlignment="Center" TextColor="White"  HorizontalTextAlignment="Center" FontAttributes="Bold" >
                    <Entry.Behaviors>
                        <behaviors:EntryLengthValidatorBehavior MaxLength="2" />
                    </Entry.Behaviors>
                </Entry>


                <!--<Label Text="Kod" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" FontAttributes="Bold"/>-->
                <Entry x:Name="tagEntry" BackgroundColor="#50FFFFFF" Keyboard="Chat" VerticalTextAlignment="Center" Text="{Binding KontrollTag}" TextColor="White"  HorizontalTextAlignment="Center" FontAttributes="Bold" >

                    <Entry.Behaviors>
                        <behaviors:EntryLengthValidatorBehavior MaxLength="2" />
                    </Entry.Behaviors>
                </Entry>

                <Entry x:Name="idEntry" IsVisible="false" Text="{Binding ID}"></Entry>
                <Entry x:Name="YearidEntry" IsVisible="false" Text="{Binding YearID}"></Entry>

                <StackLayout Orientation="Horizontal" VerticalOptions="EndAndExpand" HorizontalOptions="Center">
                    <Button Text="Ändra" FontSize="Large" BackgroundColor="#502828ff" TextColor="White" VerticalOptions="End" HorizontalOptions="Center"   Style="{DynamicResource TitleStyle}" Clicked="OnEditClicked" />

                </StackLayout>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>